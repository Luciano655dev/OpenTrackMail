# All VITE_* values are compiled into the extension and are public by design.
# Production values live in .env.production. The dev:tracking script generates
# .env.development.local for the local API and durable production pixel.
VITE_SUPABASE_URL=https://your-project.supabase.co
