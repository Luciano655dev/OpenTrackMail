// Reserved provider boundary. Outlook Web is intentionally unsupported in v1;
// implementing it later will not require changes to tracking, auth, or storage.
export const outlookSupport = "unsupported" as const;
