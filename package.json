{
  "name": "opentrackmail",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/dashboard",
    "apps/extension",
    "packages/*"
  ],
  "scripts": {
    "dev": "node scripts/dev-tracking.mjs",
    "dev:tracking": "node scripts/dev-tracking.mjs",
    "build": "npm run build -w @opentrackmail/shared && npm run build -w @opentrackmail/dashboard && npm run build -w @opentrackmail/extension",
    "test": "npm run test -w @opentrackmail/dashboard && npm run test -w @opentrackmail/extension",
    "typecheck": "npm run typecheck -w @opentrackmail/shared && npm run typecheck -w @opentrackmail/dashboard && npm run typecheck -w @opentrackmail/extension",
    "lint": "npm run lint -w @opentrackmail/dashboard && npm run lint -w @opentrackmail/extension",
    "check": "npm run lint && npm run typecheck && npm test && npm run build"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "supabase": "^2.117.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
